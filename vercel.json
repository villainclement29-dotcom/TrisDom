{
  "buildCommand": "yarn workspace app build",
  "outputDirectory": "packages/_app/dist",
  "installCommand": "yarn install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "env": {
    "YARN_ENABLE_GLOBAL_CACHE": "false"
  }
}
